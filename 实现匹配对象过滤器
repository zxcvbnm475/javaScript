在本实验中，你将创建一个函数，该函数过滤一个对象数组，并且只返回那些匹配给定源对象中所有密钥 - 值点对的对象。
目标：完成以下用户故事并通过所有测试以完成实验。
用户故事：
你应该有一个 whatIsInAName 函数，它接受两个参数，一个对象数组和一个源对象。

whatIsInAName 函数应返回一个新建数组，该数组仅包含集合中所有存在于源对象中的密钥–值点对的对象。

如果没有对象匹配源中的所有密钥–值点对，函数应返回一个空数组。
function whatIsInAName(collection, source) {
  // 获取源对象所有键
  const sourceKeys = Object.keys(source);

  return collection.filter(obj => {
    // 每个对象必须满足：源对象所有键都存在，且值相等
    return sourceKeys.every(key => obj[key] === source[key]);
  });
}
